const express = require("express");
// const {  addpackageServices, UpdateServiceFromPackage } = require("../controllers/PackageServices");
const consumerBasketDetails = express.Router();

// consumerBasketDetails.post("/addPackageService", addconsumerBasketDetails);
// consumerBasketDetails.post("/updatePackageService", UpdateServiceFromPackage);

module.exports = consumerBasketDetails;